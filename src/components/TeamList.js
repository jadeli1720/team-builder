import React from 'react';

export default function MemberList (props) {
    const {members} = props
    return (
        <div>
            {console.log('Members Array', members)}
        </div>
    )
}